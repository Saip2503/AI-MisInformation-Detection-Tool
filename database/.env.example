# --- App config ---
APP_HOST=0.0.0.0   # Host inside container
APP_PORT=8080      # Container app port

# --- Postgres settings ---
POSTGRES_USER=apiuser
POSTGRES_PASSWORD=changeme   # ⚠️ Change in production!
POSTGRES_DB=newschecks
POSTGRES_PORT=5432

# --- External services ---
NEWSAPI_KEY=your_newsapi_key_here       # Required for news fetching
TWITTER_BEARER=your_twitter_bearer_here # Required for tweets

# --- Application settings ---
MAX_NEWS_RESULTS=5
MAX_TWEETS_RESULTS=50
SIM_THRESHOLD_ARTICLE=0.65
SIM_THRESHOLD_TWEET=0.55

# ⚠️ Copy this file to `.env` and fill values
# ⚠️ Never commit `.env` with real secrets
